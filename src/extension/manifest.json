{
  "name": "Font fallback",
  "description": "Computes the font fallbacks for all the fonts loaded on the current webpage. This is helpful to prevent CLS (Web Vitals).",
  "version": "1.0",
  "manifest_version": 3,
  "background": {
    "service_worker": "js/background.js"
  },
  "action": {},
  "permissions": ["storage", "scripting", "unlimitedStorage"],
  "web_accessible_resources": [{
    "resources": ["js/shared/fonts.js"],
    "matches": ["*://*/*"]
  }],
  "host_permissions": ["*://*/"]
}